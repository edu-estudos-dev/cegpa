<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Tabela de Itens Únicos no Estoque</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .table-container {
            max-height: 65vh;
            overflow-y: auto;
            overflow-x: hidden;
        }

        .table-container table {
            width: 100%;
        }

        .table-container th,
        .table-container td {
            word-wrap: break-word;
            white-space: nowrap;
            text-align: left;
        }

        .table-container thead th {
            position: sticky;
            top: 0;
            background-color: #fff;
            z-index: 1;
        }
    </style>
</head>

<body>
<%- include('../partials/header') %>
<div class="caixa container p-4">
    <!-- breadcrumb -->
    <div class="pb-3 mt-3">
        <%- include('../partials/breadcrumb', { pageTitle: 'Estoque de Itens Únicos' }) %>
    </div>
    <div class="border caixa-externa p-4 shadow-sm rounded">
        <div class="row align-items-center mb-3">
            <h2 class="text-center text-success mb-3 mt-3 fw-bold">Estoque de Itens Únicos</h2>
        </div>
        <div class="table-container">
            <table class="table table-striped table-hover mb-0">
                <thead>
                <tr>
                    <th>Descrição</th>
                    <th class="text-center">Quantidade</th>
                </tr>
                </thead>
                <tbody>
                <% if (estoque.length > 0) { %>
                    <% estoque.forEach(function (item) { %>
                        <tr>
                            <td class="py-1 text-start"> <%= item.descricao %> </td>
                            <td class="py-1 text-center"> <%= item.quantidade %> </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="2" class="text-center">Ainda não há itens cadastrados</td>
                    </tr>
                <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>
</body>
</html>
